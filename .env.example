# LibreTranslate is hosted on Render.com, not locally.
# After deploying LibreTranslate to Render, set this variable
# in the Netlify dashboard under Site Settings > Environment Variables
LIBRE_TRANSLATE_URL=https://your-libretranslate-instance.onrender.com
